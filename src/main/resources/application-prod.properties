spring.profiles.active=prod 
#actually this part is not really needed to specify the name, cuz it actually just follow the name in the file eg: application-'name'.properties
spring.profiles.active.message=Brought to you by PROD!
logging.level.com.example.Mission8=warn
management.security.enabled=false 
management.endpoints.web.exposure.include=env,health,metrics,refresh